# Java Object-Oriented Programming Concepts

This knowledge base explains Java classes and objects, encapsulation, inheritance,
polymorphism, abstraction, interfaces, and composition. Each topic includes a Java
program with `System.out.println` statements and its expected console output.

The detailed examples are stored in `java_oop_concepts_chunks/` so the RAG index can
retrieve the most relevant concept without loading the complete guide.
